import React from "react";

const InfoPanel = props => {
    return(
        <p className="text">You have selected <span id="count">0</span> seats for a price of $<span id="total">0</span>.
        </p>
    )
};

export default InfoPanel;
